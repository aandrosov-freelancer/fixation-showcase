import 'package:flutter/material.dart';

class NoteEditorScreenWidget extends StatelessWidget {
  const NoteEditorScreenWidget({super.key, this.noteId});

  final int? noteId;

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
